api:
  endpoints:
    base-v1: /api/v1
    auth:
      base: /auth
      login: /login
      refresh: /refresh
      logout: /logout
    user:
      base: /users
      by-id: /{userId}
      password: /{userId}/password
      status: /{userId}/status
      roles: /{userId}/roles
    organization:
      base: /organizations
    tenant:
      base: /tenants
